# Copy to .env.local and fill in. Never commit a filled-in copy:
# .env* is gitignored and must stay that way.
#
# Every value here is populated in Phase 4 (backend wiring). The app builds
# and runs without them; only the API routes and Edge Function need them.

# Supabase — project URL and the browser-safe anon key.
# The anon role is INSERT-only on the two chat tables and can read nothing.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase service role. SERVER ONLY — bypasses row level security.
# Never expose this to the browser and never prefix it with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic — powers the chat widget through /api/chat. Server only.
ANTHROPIC_API_KEY=

# Resend — transactional email for lead alerts and the lead-loss fallback.
RESEND_API_KEY=

# Upstash Redis — rate limits /api/chat and /api/lead. Fails open on outage.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Google Analytics 4 measurement ID (G-XXXXXXXXXX). Public by design — it is
# visible in the page source of any GA-instrumented site.
#
# Leave this blank and the site ships no analytics at all: the GoogleAnalytics
# component is not rendered and every track() call no-ops. That is the intended
# state for local development and for previews.
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# IndexNow — the key that proves we own this host to Bing, Yandex and the
# other participating engines. NOT a secret: the same value is served at
# https://nahltech.com/<key>.txt, which is how the protocol verifies
# ownership, and public/b0b86a7cb959561bc7a1f93b95ea2055.txt is committed for
# exactly that reason. It is an env var so the ping script and the key file
# cannot drift apart silently — scripts/indexnow-ping.mjs refuses to run if
# they disagree.
INDEXNOW_KEY=b0b86a7cb959561bc7a1f93b95ea2055
